-- Keep only your personal input overrides here. Uncommented settings below
-- replace Omarchy's defaults.

hl.config({
  input = {
    touchpad = {
      -- Scroll the content, not the viewport. This matches macOS and my i3 setup.
      natural_scroll = true,
    },
  },
})
