# Per-profile secrets: license keys, tokens. See projects/README.md.
projects/*/env.local

# The generated compose override and the staged skills do NOT appear here — they
# are written to the per-workspace state directory
# (~/.local/state/devbox/<project>/), not into this tree, which the container
# mounts read-only.
