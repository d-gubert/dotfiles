# The uinput device only works while the module is loaded. systemd-tmpfiles
# creates a root-only placeholder node at boot, which kanata cannot use.
# kanata needs the real device at login, so load the module at boot.
uinput
